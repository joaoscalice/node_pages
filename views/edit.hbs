<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/style.css">
  <title>NS - Editar Página</title>
</head>
<body>
  <header>
    <h1>Editar Página</h1>
  </header>
  <div class="container">
    {{#if errors}}
      <ul>
        {{#each errors}}
          <li>{{this.msg}}</li>
        {{/each}}
      </ul>
    {{/if}}
    <form method="POST" action="/admin/edit/{{url}}">
      <textarea name="content" placeholder="Content" required>{{content}}</textarea>
      <button type="submit">Save</button>
    </form>
  </div>
  <div id="botao_voltar">
    <a href="javascript:history.back()" class="back-button">Voltar</a>
  </div>
</body>
</html>
